# kube-prometheus-stack Helm chart values
# Full monitoring stack including Prometheus, Alertmanager, and exporters

# Global settings
fullnameOverride: prometheus

# CRDs managed separately via prometheus-operator-crds chart
# to ensure proper installation order
crds:
  enabled: false

# Default rules
defaultRules:
  create: true
  rules:
    alertmanager: true
    etcd: false  # Not accessible in k3d
    configReloaders: true
    general: true
    k8s: true
    kubeApiserverAvailability: true
    kubeApiserverBurnrate: true
    kubeApiserverHistogram: true
    kubeApiserverSlos: true
    kubeControllerManager: false  # Not accessible in k3d
    kubelet: true
    kubeProxy: false  # k3d uses different proxy
    kubePrometheusGeneral: true
    kubePrometheusNodeRecording: true
    kubernetesApps: true
    kubernetesResources: true
    kubernetesStorage: true
    kubernetesSystem: true
    kubeSchedulerAlerting: false  # Not accessible in k3d
    kubeSchedulerRecording: false  # Not accessible in k3d
    kubeStateMetrics: true
    network: true
    node: true
    nodeExporterAlerting: true
    nodeExporterRecording: true
    prometheus: true
    prometheusOperator: true

# Alertmanager configuration
alertmanager:
  enabled: true
  
  alertmanagerSpec:
    replicas: 1
    storage:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 10Gi
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
      requests:
        cpu: 50m
        memory: 128Mi
  
  config:
    global:
      resolve_timeout: 5m
    
    route:
      group_by: ['alertname', 'namespace', 'severity']
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 4h
      receiver: 'default-receiver'
      routes:
        - match:
            severity: critical
          receiver: 'critical-receiver'
          continue: true
        - match:
            severity: warning
          receiver: 'warning-receiver'
    
    receivers:
      - name: 'default-receiver'
        # Default receiver - configure based on your notification needs
        # Example: webhook, email, slack, pagerduty, etc.
        webhook_configs:
          - url: 'http://alertmanager-webhook.monitoring.svc:8080/webhook'
            send_resolved: true
      - name: 'critical-receiver'
        # Critical alerts - high priority notifications
        webhook_configs:
          - url: 'http://alertmanager-webhook.monitoring.svc:8080/critical'
            send_resolved: true
      - name: 'warning-receiver'
        # Warning alerts - lower priority notifications
        webhook_configs:
          - url: 'http://alertmanager-webhook.monitoring.svc:8080/warning'
            send_resolved: true

# Grafana - disabled as we deploy it separately
grafana:
  enabled: false

# Prometheus configuration
prometheus:
  enabled: true
  
  prometheusSpec:
    replicas: 1
    
    retention: 15d
    retentionSize: ""
    
    # Enable remote write for receiving metrics from OTel Collector
    enableRemoteWriteReceiver: true
    
    # Scrape configuration
    scrapeInterval: 30s
    evaluationInterval: 30s
    
    # Resource configuration
    resources:
      limits:
        cpu: 1000m
        memory: 2Gi
      requests:
        cpu: 200m
        memory: 512Mi
    
    # Storage configuration
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 50Gi
    
    # Service monitor selectors
    serviceMonitorSelector: {}
    serviceMonitorNamespaceSelector: {}
    serviceMonitorSelectorNilUsesHelmValues: false
    
    # Pod monitor selectors
    podMonitorSelector: {}
    podMonitorNamespaceSelector: {}
    podMonitorSelectorNilUsesHelmValues: false
    
    # Additional scrape configs
    additionalScrapeConfigs: []

# Prometheus Operator
prometheusOperator:
  enabled: true
  
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 128Mi

# Node exporter for node-level metrics
nodeExporter:
  enabled: true

# kube-state-metrics for Kubernetes metrics
kubeStateMetrics:
  enabled: true

# Kube components
# Note: In k3d, control plane components run inside containers
# and are not directly accessible via standard endpoints
kubeApiServer:
  enabled: true

kubeControllerManager:
  enabled: false  # Not accessible in k3d

kubeScheduler:
  enabled: false  # Not accessible in k3d

kubeProxy:
  enabled: false  # k3d uses different networking

kubeEtcd:
  enabled: false  # Not accessible in k3d

kubelet:
  enabled: true
  serviceMonitor:
    metricRelabelings:
      # Drop high cardinality metrics
      - sourceLabels: [__name__]
        regex: 'kubelet_runtime_operations_duration_seconds_bucket'
        action: drop

